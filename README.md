# 🔗 URL Shortener on Flask

A simple URL shortening service. Paste a long link — get a short one. Built with Flask and SQLite.

## 📌 Features

- Generate short links
- Click counter
- Delete links
- Copy short link to clipboard
- CSS separated from HTML

## 🚀 Run Locally

```bash
pip install -r requirements.txt
python app.py